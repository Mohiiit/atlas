{
  "name": "karnot-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Static explorer for project research, architecture docs, and HTML case studies from Karnot Vault.",
  "scripts": {
    "sync:vault": "node scripts/sync-from-vault.mjs",
    "build": "node scripts/build-site.mjs",
    "dev": "npm run build && npx serve dist -l 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
